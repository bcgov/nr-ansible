{{'{{ with secret "apps/prod/fluent/fluent-bit" }}export AWS_ACCESS_KEY_ID="{{ .Data.data.AWS_ACCESS_KEY_ID }}"
export AWS_DEFAULT_REGION="{{ .Data.data.AWS_DEFAULT_REGION }}"
export AWS_SECRET_ACCESS_KEY="{{ .Data.data.AWS_SECRET_ACCESS_KEY }}"{{ end }}'}}

export AWS_KINESIS_STREAM="{{ aws_kinesis_stream }}"
export AWS_KINESIS_ROLE_ARN="{{ aws_role_arn }}"

# no spaces allowed! https://github.com/fluent/fluent-bit/issues/1225
export FLUENT_INPUT_LOGS_PATH="{{ logs_path }}"